# VirtualService — Best practices for authoring mocks

> Placeholder — this resource will be populated after the MVP, once we have
> observed how the agent designs mocks in real sessions and we can codify
> the patterns that work well in this codebase.

For now, refer to:

- `vs://reference/expressions` — JS scope, helpers, patterns (CRUD, errors,
  pagination, etag, master-detail).
- `vs://reference/samples` — built-in dataset shapes.
- `vs://reference/error-codes` — MCP error catalog.
